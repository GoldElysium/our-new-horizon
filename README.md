# Saba debut project

## Development

This project uses [bun](https://bun.sh/). Install it and then run the following to install the dependencies:

```shell
bun install
```

To run a local development build (running at [`http://localhost:5173`](http://localhost:5173)), run:

```shell
bun run dev
```
